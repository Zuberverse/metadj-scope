# MetaDJ Scope Environment Variables
# Copy to .env.local and fill in values

# =============================================================================
# RUNPOD DEPLOYMENT
# =============================================================================

# HuggingFace token for TURN server (WebRTC streaming in cloud)
# Create at: https://huggingface.co/settings/tokens
# Permissions: read only
# Provides: 10GB free streaming/month via Cloudflare TURN servers
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# SCOPE API (when running locally or connecting to RunPod instance)
# =============================================================================

# Scope API server URL (NEXT_PUBLIC_ prefix exposes to browser)
# Local: http://localhost:8000
# RunPod: https://your-instance-id-8000.proxy.runpod.net
NEXT_PUBLIC_SCOPE_API_URL=http://localhost:8000

# Server-only Scope API URL (used by scripts or server routes)
# SCOPE_API_URL=http://localhost:8000

# =============================================================================
# OPTIONAL: Future integrations
# =============================================================================

# Livepeer API key (if integrating with hosted Daydream API)
# LIVEPEER_API_KEY=

# Stream configuration defaults
# SCOPE_DEFAULT_MODEL=stabilityai/sd-turbo
# SCOPE_DEFAULT_WIDTH=512
# SCOPE_DEFAULT_HEIGHT=512

# =============================================================================
# OPTIONAL: Proxy Guard (only relevant if deploying publicly)
# =============================================================================

# Disable the proxy in production unless explicitly enabled
# SCOPE_PROXY_ENABLE=true

# Shared token for /api/scope proxy (NOT secure if exposed publicly)
# If set, also set NEXT_PUBLIC_SCOPE_PROXY_TOKEN to match for browser requests
# SCOPE_PROXY_TOKEN=replace-with-secret
# NEXT_PUBLIC_SCOPE_PROXY_TOKEN=replace-with-secret
